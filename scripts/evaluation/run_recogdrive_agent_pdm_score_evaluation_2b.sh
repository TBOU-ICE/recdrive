set -x

TRAIN_TEST_SPLIT=navtest
export OPENBLAS_CORETYPE=Haswell
export PATH="/opt/conda/envs/recdrive/bin:$PATH" #nby
export NUPLAN_MAP_VERSION="nuplan-maps-v1.0"
export NUPLAN_MAPS_ROOT="/mnt/datasets/recdrive/20260513/nby/recdrive/download/maps/nuplan-maps-v1.0"
export NAVSIM_EXP_ROOT="/mnt/volumes/ad-e2e-bd-su01/nby/exp"
export NAVSIM_DEVKIT_ROOT="/mnt/volumes/ad-e2e-bd-su01/nby/recdrive-scene"
export PYTHONPATH="${NAVSIM_DEVKIT_ROOT}:${PYTHONPATH:-}"
export OPENSCENE_DATA_ROOT="/mnt/datasets/recdrive/20260513/nby/recdrive/download"


export NCCL_IB_DISABLE=0
export NCCL_P2P_DISABLE=0
export NCCL_SHM_DISABLE=0

MASTER_PORT=${MASTER_PORT:-63669}
PORT=${PORT:-63665}
GPUS=${GPUS:-8}
GPUS_PER_NODE=${GPUS_PER_NODE:-8}
NODES=$((GPUS / GPUS_PER_NODE))
export MASTER_PORT=${MASTER_PORT}
export PORT=${PORT}

echo "GPUS: ${GPUS}"


# DiT / diffusion planner (Lightning ckpt with state_dict)
CHECKPOINT="/mnt/models/recdrive/v1.0.0/ReCogDrive-2B-RL/ReCogDrive_Diffusion_Planner_2B_RL.ckpt"
# Merged VLM: InternVL3-2B base (custom code) + checkpoint-400 finetuned weights
# Generated by: python scripts/evaluation/merge_internvl_base_and_checkpoint.py \
#   --base /mnt/models/recdrive/v1.0.0/InternVL3-2B \
#   --checkpoint /mnt/models/recdrive/v1.0.0/training_recogdrive_vlm_nby/checkpoint-400 \
#   --output /mnt/models/recdrive/v1.0.0/InternVL3-2B-ckpt400-merged
VLM_PATH="/mnt/models/recdrive/v1.0.0/ReCogDrive-VLM-2B"
VLM_WEIGHTS_PATH=""
# PDMS on navtest: use metric_cache (eval); metric_cache_train is for training and won't match navtest tokens.
METRIC_CACHE_PATH="/mnt/datasets/recdrive/20260513/nby/recdrive/metric_cache"

# 1. Set NAVSIM dataset and related environment variables
# 2. Configure torchrun (e.g., single machine: --nproc_per_node=8; adjust for multi-node)
# 3. Set agent.vlm_path and agent.checkpoint_path CHECKPOINT


/opt/conda/envs/recdrive/bin/torchrun \
    --nproc_per_node=8 \
    $NAVSIM_DEVKIT_ROOT/navsim/planning/script/run_pdm_score_recogdrive.py \
    train_test_split=$TRAIN_TEST_SPLIT \
    agent=recogdrive_agent \
    agent.checkpoint_path="'$CHECKPOINT'" \
    agent.vlm_path="'$VLM_PATH'" \
    agent.vlm_weights_path="'$VLM_WEIGHTS_PATH'" \
    agent.cam_type='single' \
    agent.grpo=False \
    agent.cache_hidden_state=False \
    agent.vlm_type="internvl" \
    agent.dit_type="small" \
    agent.vlm_size="small" \
    agent.sampling_method="ddim" \
    metric_cache_path="${METRIC_CACHE_PATH}" \
    agent.metric_cache_path="${METRIC_CACHE_PATH}" \
    experiment_name=eval_pdms_recdrive-rl-2b \
    worker=sequential

